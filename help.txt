docker run -d --hostname my-rabbit --name some-rabbit -e RABBITMQ_DEFAULT_USER='nik' RABBITMQ_DEFAULT_VHOST='ros' -e RABBITMQ_DEFAULT_PASS='123' rabbitmq:3-management

'amqp://nik:123@localhost/ros'

"postgresql+asyncpg://postgres:123@localhost:5432/rostelecom"

uvicorn main:app --reload

docker run --name some-postgres -p 5433:5432 -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD='123' POSTGRES_DB=ros -d postgres

export RABIT_NAME=nik
export RABIT_PASSWORD=123
export RABIT_HOST=localhost
export RABIT_PORT=5672
export RABIT_VIRTUAL=ros

docker run --name tornado -e RABIT_NAME=nik -e RABIT_PASSWORD=123 -e RABIT_HOST=172.19.0.2 -e RABIT_PORT=5672 -e RABIT_VIRTUAL=ros -p 8888:8888 mytornado